name: saludo

on:
  push:
    branches:
      - main
      - develop

jobs:
  saludar-job:
    runs-on: ubuntu-latest

    steps:
      - name: mostrar saludo
        run: echo "hola desde github actions"

      - name: mostrar perfil
        run: |
          echo " autor: ${{ github.actor }} "
      
    
      
